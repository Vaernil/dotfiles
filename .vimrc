set clipboard=unnamedplus
"ustawia taby na 4 spacje
"set tabstop=8 softtabstop=0 expandtab shiftwidth=4 smarttab
"should set tabs to 4 spaces, but I think it moves an indicator 4 spaces
"right, meaning, that the tab is actually -1 space ie instead of .1234.1234.
"it does .123.123. i feel like i should just +1 this shit
set expandtab
set shiftwidth=4
set softtabstop=4
filetype plugin indent on
" sets whitespaces, but I need some fonts or some better symbols to make it
" work first
"set listchars=eol:¬,tab:>·,trail:~,extends:>,precedes:<,space:␣
"set list
